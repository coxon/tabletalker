# Hoist Next.js peer deps so editor / type checker resolve cleanly.
shamefully-hoist=true
# Mirror for reliability in CN networks; override locally with ~/.npmrc if needed.
registry=https://registry.npmmirror.com/
