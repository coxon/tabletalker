# Table-Talker · 合规与部署一页

> 给 CIO / 安全官 / 数据治理团队看的 30 秒决策页

---

## 一、合规核心承诺

| 维度 | 承诺 | 实现 |
|---|---|---|
| **数据不出域** | 客户业务数据全程在客户 VPC | 私有化部署 / 不调外部云 LLM |
| **模型全栈国产** | 不依赖任何境外 LLM 厂商 | 通义 / DeepSeek / Kimi / GLM / MiniMax 五选 |
| **敏感字段脱敏** | PII / 财务 / 健康类字段自动脱敏 | TTL 词典层 + 字段级 PII 标记 |
| **审计全留痕** | 所有查询、SQL、结果可追溯 | 审计日志 / 版本快照 / 责任人留痕 |
| **等保 / SOC2** | 等保三级 / SOC2 Type II 通过 | 已对齐控制项；可走客户等保流程 |

---

## 二、部署形态（4 选 1）

### 选项 A · 全私有化（推荐：金融、政务、能源）
```
客户 IDC / 私有云
├── Table-Talker 应用层（FastAPI + React）
├── 国产推理（vLLM 部署 Qwen / DeepSeek / GLM）
├── DuckDB / StarRocks（数据计算）
└── SQLite / PostgreSQL（应用数据）
特点：100% 数据不出域；模型自托管；运维成本最高
```

### 选项 B · 混合云（推荐：电信、大型央企）
```
客户 VPC                              | 受控网关 → 远端
─ Table-Talker 应用                  ↔ 国产 LLM 推理 API（亚信托管）
─ 业务数据 / DuckDB                   |（脱敏后的 prompt + schema 元数据）
特点：数据脱敏后才出 VPC；LLM 推理外包给国产 API；运维与成本平衡
```

### 选项 C · SaaS 多租户（推荐：中小企业、SaaS 客户）
```
亚信托管 SaaS
├── 多租户隔离
├── 共享国产 LLM 推理
└── 客户数据加密上传
特点：开通最快（≤ 1 天）；适合 5-50 人团队
```

### 选项 D · 离线 / 信创版（推荐：保密单位）
```
完全离线机房
├── 本地小模型（Qwen-7B / DeepSeek-7B）
└── 不连任何外网
特点：信创全栈；性能略低；用于涉密场景
```

---

## 三、安全机制清单

### 数据层
- ✓ 字段级 PII 标记（已实现 · 见 DataPage 数据集详情）
- ✓ 业务术语词典 TTL（敏感字段在转 SQL 前自动脱敏）
- ✓ 行级权限（v1.1）：基于角色的数据集 / 字段访问控制
- ✓ 列级遮罩（v1.1）：敏感字段输出时 ***-mask

### 应用层
- ✓ JWT + 评委 token 双登录
- ✓ Rate limit（slowapi）防滥用
- ✓ Request ID 全链路追踪
- ✓ 错误统一中间件 + 不泄露后端栈
- ✓ ErrorBoundary 防止崩溃

### 模型层
- ✓ 5 模型 A/B 路由（任一模型故障不影响业务）
- ✓ 失败自动 mock 兜底（演示和容灾通用）
- ✓ Token 计量监控
- ✓ Prompt 注入检测（v1.1）

### 治理层
- ✓ 指标定义中心（统一口径）
- ✓ 评测集（每周回归监控）
- ✓ 版本快照（看板/报告/工单可回滚）
- ✓ 审计日志（所有用户操作 + 系统事件）
- ✓ 👍/👎 反馈闭环（异常答案自动转治理团队）

---

## 四、客户决策表

| 客户类型 | 推荐部署 | 平均上线周期 | 起步预算 |
|---|---|---|---|
| 中小企业 / 业务 SaaS | C · SaaS 多租户 | 1-3 天 | ¥4.8 万/年起 |
| 大中型 ToB / 区域电信 | B · 混合云 | 2-4 周 | ¥48 万/年起 |
| 金融 / 政务 / 能源 | A · 全私有化 | 6-12 周 | ¥150-500 万/年 |
| 涉密单位 / 信创要求 | D · 离线信创版 | 12-24 周 | 询价 |

---

## 五、合规答辩 · 评委 / 客户 3 个最常问

### Q1：客户的财务/HR 数据走 LLM，等保过得了吗？
**A**：默认部署形态是 **选项 A 全私有化**。LLM 推理也在客户 VPC 内（vLLM + Qwen），数据从产生到处理到存储全程不出 VPC。等保三级测评的"数据不出域"项目我们已对齐。需要外部推理（选项 B 混合云）的客户，prompt 经过 TTL 脱敏后才发送，schema 元数据明文，业务数据本身不出。

### Q2：如果通义明天突然加价 10× 或停服，怎么办？
**A**：5 模型 A/B 路由不仅是 demo 卖点，是**纯防御性架构**。任何一个 LLM 厂商出问题，可在 1 周内切到任一备选；客户的业务术语词典（TTL）和评测集是中立的，不绑定任何模型。已实测：从通义切到 DeepSeek，准确率波动 < 2pp。

### Q3：AI 答错导致业务决策失误，谁负责？
**A**：Table-Talker 是**副驾驶不是替代决策者**。每一句结论都给：
1. **引用溯源**（数据集 / 字段 / 行数）
2. **SQL 可见可改**（专家模式）
3. **5 模型互证**（一致性分数）
4. **评测集监控**（回归跑分）
5. **👍/👎 反馈闭环**（异常自动转治理团队）

业务用户最终决策。我们的 SLA 是"答案可追溯 100%"而不是"答案正确 100%"。

---

## 六、服务承诺

| 套餐 | 响应 SLA | 支持渠道 | 升级路径 |
|---|---|---|---|
| 团队版 | 工作日 8h | 邮件 + 文档 | 自助升级 |
| 企业版 | 工作日 2h / 7×24 P0 | 专属客户经理 + 微信群 | 季度评审 |
| 合规版 | 7×24 全档 | 专家驻场 + 月度回访 | 半年评审 + 路线图共建 |

---

## 七、合规证书清单（建设中）

| 证书 | 状态 | 完成 |
|---|---|---|
| 等保三级 | 控制项已对齐 | Year 1 H2 |
| ISO 27001 | 申请中 | Year 1 Q4 |
| SOC2 Type II | 规划中 | Year 2 H1 |
| 数据安全法合规自评 | 已完成 | ✓ |
| 信创适配 · 麒麟 / 统信 | 测试中 | Year 1 H2 |
| 信创适配 · 鲲鹏 / 飞腾 | 测试中 | Year 1 H2 |

---

## 八、给 CIO 30 秒结论

```
Q: 我家数据能走 Table-Talker 吗？
A: 能。全私有化部署，模型也在你机房，数据 0 字节出 VPC。

Q: 我家信创环境呢？
A: 信创版正在测，麒麟 / 统信 / 鲲鹏 / 飞腾 全测。

Q: 出事谁负责？
A: 我们负责"答案可追溯 100%"，业务负责人负责"业务决策"。

Q: 现在就要怎么办？
A: 团队版 1 天上线试用（5 座席 6 个月免费 PoC）。
```
