# Table-Talker · 安全模型说明

> 给评委 / CIO / 安全审计的快速答辩页

---

## 一句话定位

**前端 = UX 防护层 · 后端 = 真权限层**

前端的 RBAC、PII 拦截、审计日志，**都是 UX 体验**而不是真鉴权。
真鉴权 100% 在后端 FastAPI middleware + 数据库 row-level security。

---

## 为什么这么做（标准 SaaS 安全实践）

| 层 | 责任 | 例子 |
|---|---|---|
| **前端** | 立刻反馈、避免无效请求、提升体验 | PII 关键词命中立刻 toast；Viewer 看不到删除按钮 |
| **后端 middleware** | 真正鉴权、审计、限流 | JWT 验证 → 角色 → DB role 校验 |
| **数据库** | 行级权限、字段遮罩 | RLS policy + column-level GRANT |

> 如果用户改 localStorage 把 role 改成 admin：
> - 前端 UX 会让他看到"按钮"
> - **但后端会立刻 401** 因为 JWT 里的 role 是 admin 时签发的、不可伪造
> - 数据库 RLS 会再拒绝一次

---

## 评委一定会问的 3 个问题

### Q1 用户改 localStorage role 怎么办？
A：前端权限是**视觉提示**，真权限在后端。JWT 不可伪造，DB RLS 不可绕过。
   前端检查只是为了让用户**少做无效操作**，不是把守。

### Q2 PII 拦截前端怎么挡？
A：前端**第一道**（关键词正则 + 同义词扩展），命中立刻拒绝并写审计。
   后端**第二道**（语义分析 + 字段类型检测）。
   数据库**第三道**（敏感字段在 Viewer 角色下永远返回 mask）。
   三道防御 · 任何一道生效都不会泄漏。

### Q3 审计日志能被篡改吗？
A：前端 localStorage 的审计**只是缓存** — 用户可清可改。
   真审计在 SQLite + 对象存储 + 不可变追加日志（append-only）。
   合规版部署时连接到客户的 SIEM 系统（Splunk / 阿里云日志服务）。

---

## 当前 demo 实现状态

| 防御层 | demo 状态 | 真上线状态 |
|---|---|---|
| 前端 PII 关键词 | ✓ 已实现（27 关键词 + 同义词扩展）| 同 |
| 前端审计 toast | ✓ 写 localStorage `tt_versions_v1` | 改写后端 SQLite |
| 后端 JWT middleware | ✓ 已实现 `auth.py` | 同 |
| 后端 role check | ✓ middleware 装饰器 | 同 |
| 数据库 RLS | ⚠ 演示版 mock | v1.1 接 PostgreSQL RLS |
| 字段级遮罩 | ⚠ UI 展示 | v1.1 接 DuckDB column-level |
| 审计 append-only | ⚠ SQLite 简单表 | v1.1 接对象存储 WORM |
| 不可变 hash chain | ✗ | v1.2 |

---

## 给评委的 30 秒答辩

```
"前端 RBAC + PII 拦截 是 UX 体验，不是把守门的。
 真权限在后端 JWT + 数据库 RLS。
 用户改前端 localStorage 不会越权 — 后端会再拦一次。
 这是行业标准实践（参考 ThoughtSpot / Tableau / Hex 都是这样）。"
```
