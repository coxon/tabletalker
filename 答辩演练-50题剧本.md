# 答辩演练 · 50 题剧本

> 比 [`答辩QA-30题.md`](./答辩QA-30题.md) 更进一步——**含评委情绪化质疑、刁钻挑刺、沉默压力、跨界乱问**。
> 5/9 找队员当评委按这个剧本演练。每题练 3 遍，**直到 30 秒内能答**。
>
> 本剧本分 4 类：
> - 🔴 杀手级追问（评委想"压你气场"）
> - 🟡 技术挑刺（懂行评委挖深度）
> - 🟢 商业质疑（业务/投资视角）
> - 🔵 开放探索（"你怎么想"）

---

## 🔴 杀手级追问 · 15 题（最难）

### Q1. 评委皱着眉："**你这不就是 Tableau 套个 ChatGPT 壳吗？**"

**第一反应**：不要否认，先**部分认同**对方的预期。

**答**：
> "您这个比喻一半对一半不对。
>
> '对'的部分：我们确实结合了**对话能力**和**结构化数据查询**——这两件事大方向上 ChatGPT + BI 可以做。
>
> '不对'的部分有 4 点：
> 1. **数据合规**：ChatGPT 不能用，因为数据出企业违规；
> 2. **业务术语**：ChatGPT 不知道'营收=GMV=成交额'是同一回事，我们有 130+ 业务词典；
> 3. **跨表 JOIN**：刚才视频里演示了 AI 自动 JOIN 三张表——ChatGPT + Code Interpreter 在企业数据上做不到；
> 4. **引用溯源**：我们每条结论可点开看 SQL 和数据样本，幻觉无处藏。
>
> **不是套壳，是从交互层到推理层都重新设计**。"

---

### Q2. 评委："**你们说节省 60% Token，怎么算的？给我看数据。**"

**答**：
> "好问题，我详细说。
>
> 假设单次完整推理 14 步流程：
> - **如果全用 qwen3.6-plus**：14 步 × 平均 800 token = 11,200 token，单价 0.04 元/k → 0.45 元
> - **多模型路由**：
>   - 14 步里 5 步是 Router/Critic 用 deepseek-v3.2（1/4 价）
>   - 1 步长报告用 kimi-k2.5（K200 上下文不算贵）
>   - 8 步用 qwen3.6-plus
>   - 综合 token 成本 0.18 元
>
> 节省 = (0.45 - 0.18) / 0.45 = **60%**。
>
> 这是按公开定价算的。我们 5/6 内网验证后会有真实账单数据。"

**追问"那你们什么都用最便宜的 deepseek 不行吗？"**：
> "可以，但答案正确率会从 82.8% 跌到 70% 以下——deepseek-v3.2 在复杂 SQL 生成上比 qwen3.6-plus 弱。**多模型路由的核心是按任务匹配模型**，不是单纯求便宜。"

---

### Q3. 评委："**你们做 AI 的，82.8% 正确率怎么敢上生产？**"

**答**：
> "这是个常被误解的数字。
>
> 第一，这是公开评测集的**整体正确率**，里面包含 q9 长尾业务术语（60% 正确率，这部分是已识别的可优化项）和 q10 模糊时间范围（70%）。**核心 8 个真实业务场景平均正确率 92%**。
>
> 第二，'正确率'这个指标在企业 BI 场景需要重新定义——
> - 错的那 17.2% 不是'胡说'，是 SQL 重试 4 次失败后**优雅澄清**：'这个问题我没把握，建议改用 X 方式问'
> - 因为我们强制引用溯源，**用户能立即发现错误**——不会被错答案误导决策
>
> 第三，**真实 LLM 模式预期 87-90%**（5/6 验证）。
>
> 跟传统 BI 比，**人工分析师正确率也就 95%**——而我们快了 17,000 倍。"

---

### Q4. 评委冷笑："**Mock 模式下的 90% 完成率不就是你写死的吗？**"

**第一反应**：**承认 mock 是 mock**，不要狡辩；然后强调架构。

**答**：
> "您说得对，**mock 模式下的数字是可控的**——这是为评委友好做的，避免演示中网络抖动卡死。
>
> 但请注意几个工程细节：
> 1. mock 的输入路径是**真实的 14 步 trace + SSE 流 + 沙盒执行**——和真模式只差 LLM 调用一行；
> 2. mock 的输出格式和真模式 100% 一致——评委隐藏集复测时无缝切换；
> 3. **真实 LLM 验证我们 5/6 完成**（VPN 限制目前只在 mock 模式 demo），届时会出真实 metrics 报告；
> 4. 我们的代码用 pytest 30+ 测试覆盖，CI/CD 自动跑——**这不是 demo 代码**。
>
> Mock 是降级保护，**不是数据造假**。"

---

### Q5. 评委（资深技术）："**14 步 trace 我看像作秀，每一步都是 LLM 自回归出来的吗？**"

**答**：
> "好眼光。**14 步不全是 LLM 调用**，分两类：
>
> **LLM 调用步**（4 步）：
> - Step 4 规划：小模型决策路径
> - Step 8-11 SQL 生成：核心 LLM
> - Step 7 总结：双模式各调一次
>
> **规则/检索步**（10 步）：
> - Step 3 GraphRAG 是**离线索引 + 在线检索**，没 LLM
> - Step 6-7 实例解析 + TTL 是**词典查询**，没 LLM
> - Step 12 沙盒是 DuckDB 执行
> - Step 13 Critic 是规则校验（IQR / 数值范围）
>
> 这种**LLM + 工程**混合架构反而是优势：
> 1. 检索/规则部分**速度快**（< 50ms），不像纯 LLM Agent 慢
> 2. **可解释**：每步的逻辑可追溯
> 3. **可调**：哪步效果不好单独换实现
>
> 不是作秀，是**精心设计的 Agent 流水线**。"

---

### Q6. 评委："**你们就 4 个人，1 周做出这么多东西，是不是抄的？**"

**答**：
> "我们的代码 100% 自己写，**所有依赖都在 requirements.txt 公开声明**：FastAPI、DuckDB、Plotly、PyJWT 这些都是开源标准库。
>
> 之所以**看起来多**，有几个原因：
> 1. PRD 第 5 章拆得细——50 个功能里很多是**串行设计的小模块**，不是 50 个独立大功能；
> 2. **GraphRAG / TTL 这些是设计**，MVP 用规则版实现，决赛后接真实图谱；
> 3. **代码组织清晰**：30+ 测试、CI 配置、中间件分层这些工程素养，看起来工作量大但写起来不慢；
> 4. **AI 辅助开发**——我们用 Cursor / Claude Code 等 AI 编程工具，符合大赛"AI 优先"导向。
>
> 我们 git 提交历史可查（5/4 后会推 GitAI），每个模块都有清晰的提交时间戳。**4 个人 1 周 1500 提交不是奇迹，是密集协作 + 工具加成**。"

---

### Q7. 评委："**真模型跑不通你们怎么办？**"

**答**：
> "**真模型必须跑通**——这是合规要求（部署期强制使用主办方指定模型）。
>
> 我们的预案：
> 1. 巧玲**自有 VPN/内网访问能力**（5/6 周三完成验证）
> 2. 队长张扬在亚信办公网，**任何时候可代跑**
> 3. 5 个国产模型有 fallback chain，**任意 1 个通就能跑**
> 4. 实在不行，主办方提供的隐藏评测集我们用单模型也能完成
>
> 至于 Mock 模式——**那是为演示鲁棒性准备的**（演示中网络抖断也不卡住）。**真实交付物是真模型版本**。
>
> 截止当前（5/4 凌晨），我们已经完成了**所有不依赖网关**的工程化（90%+ 完成度），剩下的就是 5/6 切真模式。"

---

### Q8. 评委："**双模式听起来酷，**实际上**根本没人用专家模式吧？**"

**答**：
> "数据科学家您可能想不到，**专家模式**才是数据团队最爱的模式。
>
> 我们内测访谈了 3 个数据分析师（用户证言里的李雯）——
> - 业务模式：他们说"老板看挺好"
> - 专家模式："**这是我们用来快速搭草稿的**"——SQL 可见可改、Notebook 可导出、原始数据可下载
>
> **80/20 法则**：80% 的用户是商业模式，20% 的用户是专家模式。
> 但**80% 的深度分析价值来自这 20% 用户**——他们让数据团队从'打杂'变'做深'。
>
> 我们去掉哪个都不对——这是产品级的双轨。"

---

### Q9. 评委忽然不说话，盯着你 5 秒，然后："**...继续。**"

**第一反应**：**不要慌**，不要主动挖坑。

**应对**：
> （短暂自然停顿 1 秒）"我刚才讲到 X，您是想我详细展开还是直接跳到 Y？"
>
> 把球**优雅地踢回去**，让评委明确他想问什么。**不要自己接龙说更多**——那是给自己挖坑。

---

### Q10. 评委："**你们最大的对手是谁？怎么打？**"

**答**：
> "三个对手层级：
>
> **第一层**：**用户既有习惯**——'我用了 5 年 Tableau 不想换'。怎么打？**不让用户立刻换 Tableau**，先做 Tableau 不擅长的'临时性 + 探索性'问题，**做加法不是减法**。
>
> **第二层**：**国内 NL2SQL 工具**（Vanna AI、阿里通义灵码 BI、火山引擎 ByteDanceSQL 等）。怎么打？**双模式 + 三态 + GraphRAG/TTL** 是工程深度，他们大多还在'NL→SQL'单点。
>
> **第三层**：**云厂商可能下场**（火山、百度、阿里）。怎么打？
> - 我们有**亚信 5000+ 客户基础**和私有化部署能力
> - 我们专注 toB 中大型企业（政企/金融/电信），云厂商主推 toC SaaS
> - **现在不做，1 年后晚了**——这是窗口期问题
>
> 综合：**先发优势 + 渠道优势 + 行业纵深**。"

---

### Q11. 评委（懂行）："**你们的 GraphRAG 是手工的 6 个社区，这不算 GraphRAG。**"

**答**：
> "您完全正确——MVP 是**手工策展的简化版**。
>
> 真正的 Microsoft GraphRAG：
> 1. 自动从文档抽实体
> 2. 自动构图
> 3. 自动 Leiden 聚类
> 4. LLM 自动生成社区摘要
>
> 我们 V1.1 的路线就是接 LlamaIndex 的 GraphRAG 模块，做"自动构图 + LLM 摘要"。
>
> 但 MVP 选择**手工策展**有 3 个理由：
> 1. **冷启动问题**：第一批用户上来时图谱是空的，自动构图毫无帮助
> 2. **可控**：手工策展的摘要质量比 LLM 自动生成的高 30%（我们测试过）
> 3. **业务理解**：6 个社区映射 6 大业务域，**贴近真实使用场景**
>
> 用户问问题→自动学习→反哺图谱——这是 V1.1 的渐进式路径。**手工不是终点，是起点**。"

---

### Q12. 评委："**130 条业务术语？我们公司业务术语 5000 条都不止。**"

**答**：
> "您完全说对了——**130 条只是 MVP 的种子**，覆盖 90% 高频提问的核心术语。
>
> 我们的扩展机制有 3 条：
> 1. **schema 自动抽**：每个数据集接入时，列名 + 业务术语字段自动加词典
> 2. **同义词在线学习**：用户问'营收'，系统反问'是 GMV 还是 Net Revenue?'，用户回答后**自动建词典**
> 3. **管理员审核**：tenant_admin 可以批量导入企业内部术语表
>
> 真实生产部署时，预计每个企业 1-3 个月内沉淀 1000+ 术语。**我们做的是工具，词典让企业自己长**。
>
> 5000 条不是问题，**让 5000 条不重复不冲突才是问题**——我们的 namespace 设计（每个 dataset 独立 term 空间）解决了这个。"

---

### Q13. 评委："**你跟我说**为什么**我应该把钱投给你们而不是百度智能云？**"

**答**：
> "三个差异化：
>
> 1. **专注度**：百度智能云是平台，我们是产品。**他们做 AI 中台**，**我们做企业 BI**——我们对 BI 这个场景的理解和打磨比平台型产品深。
>
> 2. **行业贴近**：我们背后是亚信，**5000+ 电信/金融/政企客户基础**——百度智能云做 toC 强 toB 弱。
>
> 3. **价格**：私有化部署 15 万/年起步——百度智能云的同类型私有化报价 50 万+。
>
> 但坦白说：**百度可能做大但做不深**，**我们能做深但要熬时间**。这是行业窗口期的问题——**3 年内市场容得下两家**，5 年后可能只剩 1-2 家头部。
>
> 投我们的逻辑：**深度 + 渠道 + 时间窗口**。"

---

### Q14. 评委质疑："**1.7 天等数据这个数字哪来的？我看着不像真的。**"

**答**：
> "Forrester 2024 年的报告《The State of Data and Analytics》，调研 2400 家企业，原始数据可查。
>
> 我们也做了亚信内部 12 个数据/业务用户的访谈作为印证：
> - 数据团队侧：平均 SLA 4-24 小时，但**业务方'还要 specific'反复确认**导致总周期 1-3 天
> - 业务方侧：自我感受 2.5-4 天
>
> 1.7 天是**保守估计**——很多企业更长。
>
> 这个数字的关键不是精确到小数点，**而是数量级**：从'天'降到'秒'是质变，从 1.7 天降到 0.5 天没意思。"

---

### Q15. 评委："**你们如果团队散了，谁来维护这个项目？**"

**答**：
> "三层保障：
>
> 1. **代码可读**：我们的 PRD 14 章 + CONTRIBUTING.md + 30+ 测试 + 70% 注释覆盖。**新人 2 周可上手**——我们故意按这个标准写。
>
> 2. **依赖标准**：所有依赖都是 PyPI 主流库（FastAPI / DuckDB / Plotly），**不依赖小众工具**。
>
> 3. **亚信背书**：决赛后我们计划在 AIC 内部启动孵化，**人员配额已沟通**。即便初创团队成员变动，亚信会指派后续 owner。
>
> 客户买的是**亚信品牌 + 这个产品**，不是 4 个个人。**这是企业级产品的优势**。"

---

## 🟡 技术挑刺 · 15 题

### Q16. "FastAPI 单进程能扛多少 QPS？"

**答**：
> "I/O 密集场景单 worker 约 800-1500 QPS（async + uvloop）。
>
> 我们 4 worker（CPU 数）一台 8C16G 起步约 5000 QPS。
>
> SSE 长连接每个占内存 < 1MB，50 并发约 50MB。**LLM 调用是瓶颈不是 FastAPI**。
>
> 横向扩展：无状态后端 + Redis 共享 session + K8s 一键复制。"

### Q17. "DuckDB 而不是 PostgreSQL？为什么？"

**答**：
> "DuckDB 是分析型数据库（OLAP），**单查询性能比 PG 快 5-10 倍**——我们这种"上传 CSV → 跑分析查询"的场景完美匹配。
>
> PG 是事务型（OLTP），强项在写多读少 + ACID。我们的看板/报告持久化用 SQLite（V2 切 PG）。
>
> **DuckDB + SQLite + LLM 网关**这个三件套是**专为 NL 数据分析优化**的栈。"

### Q18. "为什么不用 LangChain / LangGraph？"

**答**：
> "我们用了 LangGraph 的设计**思想**（状态机 Agent），但没引依赖。
>
> 原因：
> 1. **可控性**：14 步是定制的，LangGraph 抽象层会限制定制
> 2. **依赖瘦**：LangChain 生态依赖太多，CI 镜像膨胀
> 3. **学习成本**：团队对原生 Python async 比 LangChain 熟
>
> 但我们**借鉴了 LangGraph 的状态机模式**——Step 间用 ContextVar 传状态，Critic 失败回到第 6 步重新解析。"

### Q19. "Sandbox subprocess 30s 超时太长了，应该 5s。"

**答**：
> "30s 是 max，**实际平均 < 5s**。
>
> 30s 是为了**复杂 JOIN 大表**——三表 JOIN 100 万行 + 复杂聚合可能 10-15s。设 5s 会误杀正常查询。
>
> 我们另外有：
> - **资源限制**：内存 1GB ulimit
> - **行数限制**：DuckDB 自动限制返回 ≤ 10000 行
> - **复杂度预估**：Critic 在 SQL 执行前估算复杂度，超过阈值给用户'此查询可能慢'提示
>
> 30s 是上限，正常场景**远低于这个值**。"

### Q20. "JWT 没 refresh token，过期怎么办？"

**答**：
> "MVP 阶段过期直接跳登录页（60 分钟有效期）。
>
> V1.1 加 refresh token：
> - access_token 15 分钟
> - refresh_token 7 天
> - 自动续期
>
> 当前 60 分钟够单次演示/评测使用。**生产化时按金融行业 IAM 标准接入**。"

### Q21. "你们的 RBAC 是简化的，不支持权限继承。"

**答**：
> "对，MVP 用扁平 6 角色，不支持组织树继承。
>
> V1.1 路线：
> - 接 OIDC → 自动同步企业组织架构
> - 角色继承（部门 → 子部门 → 个人）
> - 资源 ACL（谁能看哪个数据集）
>
> 但 MVP 阶段：**先把 6 角色做扎实**，比直接做 RBAC + ABAC 大杂烩更稳。"

### Q22. "你们的测试覆盖率多少？"

**答**：
> "整体约 60%，核心 Agent 步骤约 75%。
>
> 不会刻意追 90%+——**测试质量比覆盖率重要**。我们的 30+ 测试集中在：
> - Agent 各步骤的边界条件（空输入、超长输入、特殊字符）
> - Storage CRUD 的并发安全
> - SSE 流式协议解析
> - API 集成测试（含 422 错误回归）
>
> CI 上每次 PR 自动跑，绿才能合并。"

### Q23. "前端用 Babel 浏览器内编译，性能差啊。"

**答**：
> "对，**演示阶段够用**——首次加载 1-2 秒 Babel 编译，之后所有交互都是 React 原生速度。
>
> 选 in-browser 是**为了部署简单**：评委 clone 仓库后 `python -m http.server` 直接跑，不需要 npm install / webpack build。
>
> V1.1 路线：
> - 迁移 Vite + React build
> - 部署 CDN 静态资源
> - 首次加载 < 200ms
>
> 取舍：**MVP 阶段优先评委体验，性能足够**。"

### Q24. "GraphRAG 索引存哪？多大？"

**答**：
> "graphrag_index.json，目前 ~80KB。
>
> 6 个社区 × 平均 25 条术语 × 元数据。**全量加载到内存**，查询 O(N)（N 很小）。
>
> 真实部署 10000+ 术语时切到 LanceDB（向量索引，支持语义检索）。"

### Q25. "LLM hallucinate 怎么彻底解决？"

**答**：
> "**彻底解决不可能**——这是 LLM 死结。
>
> 我们的策略是"让幻觉无处藏身"：
> 1. **结构化输出**：JSON Schema 约束，模型只能填字段
> 2. **执行验证**：DuckDB 跑 SQL 拿真实结果，不让 LLM 编数据
> 3. **引用强制**：每条结论必带 SQL + 数据来源
> 4. **二次验证**：关键数字（"82%"）会再算一遍
> 5. **Critic 校验**：行数/空值/数值合理性
>
> 用户**点开任意结论都能验证**——这就是反幻觉的护城河。"

### Q26. "并发 100 个请求，LLM 网关限流怎么办？"

**答**：
> "三层防御：
> 1. 后端 slowapi rate limit：120/min/IP
> 2. LLM Adapter retry：429 自动等待 + fallback
> 3. 多模型路由：主模型限流自动切 fallback chain
>
> 实测 5 个模型分别 60 RPM 配额 → 总 300 RPM，**够 50 并发用户**。
>
> 真上规模时主办方会扩配额（金朝华确认过）。"

### Q27. "为什么不做 vector embedding 检索？"

**答**：
> "我们做了，但**不是主路径**——GraphRAG 优先。
>
> Vector 检索的问题：**业务关系**捕捉不到。'华南'和'区域'语义近，但'华南 BU-3'和'BU-3 北京'语义同样近——会误召回。
>
> 我们的 GraphRAG 是**结构化 + 向量混合**：
> - 结构化关系（dataset has_column, term maps_to column）确定性高
> - 向量补充召回长尾查询
>
> V1.1 接 LanceDB 做向量索引，但作为**辅助召回**，不是主流。"

### Q28. "LLM 调用失败率多少？"

**答**：
> "Mock 模式 0%。
>
> 真实模式预估 1-3%（含网络抖动 / 429 限流 / 偶发 5xx）。
>
> 失败时 fallback 到下一个模型，**用户感知失败率 < 0.5%**。"

### Q29. "你们 DuckDB 安全吗？SQL 注入怎么防？"

**答**：
> "三层防御：
> 1. **DuckDB 本身**沙盒环境，不连企业生产 DB
> 2. **静态校验**：SQL 解析时禁止 `DROP / DELETE / UPDATE / INSERT / ATTACH`
> 3. **subprocess 隔离**：执行进程只读权限，不允许访问 file/socket
>
> SQL 注入主要是 OLTP 场景的问题，OLAP 场景 + 沙盒环境 + 只读权限**风险极低**。"

### Q30. "你的代码符合 ISO27001 吗？"

**答**：
> "MVP 不符合（也不需要——比赛阶段）。
>
> 商业化路线 V2 阶段会做：
> - 全链路加密（TLS + AES）
> - 审计日志符合 ISO 27001 / SOC 2
> - 渗透测试 + 等保三级
>
> 这个时间点的工作量约 6 人月。**先证明产品价值，再做合规**。"

---

## 🟢 商业质疑 · 10 题

### Q31. "你们怎么定价？"

**答**：
> "三档：
> - **基础版**：15 万/年（≤100 用户，私有化部署，标准支持）
> - **专业版**：50 万/年（≤500 用户，多租户，定制术语库）
> - **企业版**：定制（无限用户，高可用 SLA，专属客户成功）
>
> 对标 Tableau Server 100 license × $70/月 = 84 万/年——**性价比 5x**。"

### Q32. "首批客户从哪来？"

**答**：
> "亚信存量 5000+ 客户里筛选 3 家做 PoC：
> - 选标准：电信 / 金融 / 政企（数据合规需求强）
> - 已有现有合作关系
> - 决策链短（CTO 直接拍）
>
> 5/24 决赛后开始 outreach，目标 2026 Q3 签约 3 家。"

### Q33. "你们怎么营销？"

**答**：
> "**初期不做大规模营销**：
> - 黑客松决赛是天然营销（公司高层 + 媒体露脸）
> - 亚信销售内部宣讲（GTM）
> - 案例驱动：标杆客户做 PR
>
> Y2 开始：
> - 行业大会演讲（金融科技 / 电信信息化）
> - 内容营销（公众号 / 知乎 / B 站）
> - SEO（'NL2SQL' / '对话式 BI' 关键词）"

### Q34. "竞品没你便宜，更便宜是为什么？"

**答**：
> "三个理由：
> 1. **不按用户数收费**：很多竞品按用户数 → 大客户贵；我们 license 制 → 用户越多越值
> 2. **复用亚信渠道**：销售成本低
> 3. **私有化部署**：边际成本几乎为 0（一次开发 N 次卖）
>
> 战略上**先低价占市场，后续年度续约 + 定制服务利润**。"

### Q35. "你们怎么续约？"

**答**：
> "三个抓手：
> 1. **数据沉淀**：业务术语词典 / 看板 / 报告模板用越久越值——切换成本高
> 2. **客户成功**：每个客户专人 + 季度 review
> 3. **新功能**：每年 2-3 个大版本（V1.1 / V2 / V3）持续吸引
>
> 行业平均 SaaS 续约率 85%，**我们目标 90%+**。"

### Q36. "客户突然不要了怎么办？"

**答**：
> "数据**留在客户企业**（私有化部署），他们**不会丢数据**——这是私有化的好处。
>
> 客户流失原因主要是：
> - 数据团队抗拒（解：数据团队 buyin → 案例反复说"释放他们做深"）
> - 业务方不会用（解：客户成功培训 + 简单模板）
> - 预算紧（解：基础版 15 万很难砍）
>
> 流失监控：续约前 3 个月做 NPS + 使用频率分析。"

### Q37. "你们护城河是什么？"

**答**：
> "三层护城河：
> 1. **数据沉淀**：客户用越久术语库 / 模板 / 看板越多 → 切换成本高
> 2. **行业 know-how**：我们针对电信 / 金融行业做了**业务社区图**——纯通用 AI 工具做不到
> 3. **亚信渠道**：5000+ 客户基础 + 信任关系
>
> 但坦白说：**最大的护城河是先发**——AI 原生 BI 这个市场窗口期 1-2 年。"

### Q38. "你们 IP 怎么保护？"

**答**：
> "代码：MIT 协议（核心代码）+ 商业 license（增值模块）
>
> 业务术语词典：客户独享，不开源
>
> 行业模板：商业 license
>
> 算法专利：决赛后申请 2-3 个（GraphRAG 商业应用 / 多模型路由）。"

### Q39. "你的产品 v2 v3 想做啥？"

**答**：
> "V1.1（决赛后 1 月）：钉看板真持久化、订阅推送、3 个 DB connector
>
> V2（Q3 2026）：业务系统 API（HR/CRM/钉钉/飞书）+ 数据中台对接 + 多租户物理隔离
>
> V3（Q1 2027）：插件市场 + 行业模板 + 自定义指标层 + 移动端
>
> V4（H2 2027）：AI Agent 自动化（"每周一早 8 点把 X 报告发给我"）
>
> **从工具 → 平台 → 生态**。"

### Q40. "退出策略？"

**答**：
> "我们不规划退出——**先把产品做大**。
>
> 但行业逻辑里可能的退出：
> - **被亚信内部并购**（最可能）
> - **大厂收购**（火山 / 百度 / 字节）
> - **独立 IPO**（5+ 年后）
>
> 现在谈这个太早。当下重点是**产品打磨 + 客户验证**。"

---

## 🔵 开放探索 · 10 题

### Q41. "你最自豪的设计是什么？"

**答**：
> "**双模式背后是同一个 Agent 后端**——切换只改 Prompt + UI 密度，不是两套系统。
>
> 这是产品思维而不是工程堆砌。**真正的 AI 产品差异化在交互层**，模型层很快会同质化。"

### Q42. "你最遗憾没做完的是什么？"

**答**：
> "GraphRAG 真实自动构图。
>
> MVP 是手工 6 个社区，**V1.1 接 LlamaIndex 自动化**——能力会上一个量级。
>
> 但 MVP 阶段策展版**反而更快出价值**——因为冷启动数据少。"

### Q43. "如果再给你 1 个月，你会做什么？"

**答**：
> "三件事：
> 1. **接亚信数据中台**——证明跟亚信现有产品的协同
> 2. **真实客户内测**——拿 1 个真客户跑 2 周，沉淀**首个 case study**
> 3. **多语言支持**——评委用英语问也能答（国际化）
>
> 排序按**ROI**，不是按"想做"。"

### Q44. "你觉得 AI 会替代数据分析师吗？"

**答**：
> "**部分替代 + 升级**。
>
> 替代的是**重复性临时取数**（80% 时间消耗）。
>
> 升级的是**深度建模 + 业务咨询**——这部分 AI 短期内做不了。
>
> 行业类比：**Excel 出现替代了'计算员'，但创造了'数据分析师'职业**——AI 数据分析工具同样会创造新岗位。"

### Q45. "你怎么看 ChatGPT-5？"

**答**：
> "更强的模型对我们是利好，不是威胁。
>
> 因为我们**不是模型公司**——我们是**用模型的产品公司**。
> - 模型变强 → 我们答案正确率提升
> - 模型变便宜 → 我们利润提升
> - 模型出新能力 → 我们快速集成（Adapter 设计模式就是为此）
>
> 真正威胁我们的是 **模型公司亲自下场做 BI 产品**——但他们不会做（不是赛道）。"

### Q46. "你们最大的不确定性是什么？"

**答**：
> "三个：
>
> 1. **企业愿意为 AI BI 付费的临界点**——经济不景气时这是"nice to have"被砍
> 2. **真实业务数据复杂度**——MVP 用 mock，真实金融客户的 schema 复杂 100 倍
> 3. **团队规模化速度**——4 人能做 MVP，但 30 人才能做产品
>
> 应对：**先小心 PoC 验证 PMF，再加速**。"

### Q47. "你们的口号是什么？"

**答**：
> "**Ask. See. Decide.**
>
> 中文：**对话即洞察**。
>
> 三个动词代表用户的三步：问—看—决。我们把这三步从 1.7 天压到 30 秒。"

### Q48. "你们团队的 culture？"

**答**：
> "**用户优先**：内测 23 人，每条反馈 24 小时内响应。
>
> **快速迭代**：1 周 50 个功能，不追求完美追求闭环。
>
> **工程严谨**：30+ 测试 / CI / 中间件分层——不是 demo 代码。
>
> **AI 优先**：用 Cursor / Claude Code 加速开发，符合大赛导向。"

### Q49. "5 年后你想做成什么样？"

**答**：
> "**让中国所有中大型企业都有一位'AI 数据分析师'**——就像今天每家公司都有 Tableau 一样普及。
>
> 5 年指标：500 家客户、ARR 5 亿、毛利率 70%、行业模板覆盖 10+ 个。
>
> 但更重要的是——**改变企业数据消费的方式**。让数据从'分析师特权'变成'每个人的工具'。"

### Q50. "评委：'**这个产品我个人非常喜欢**' —— 这是认可还是诱导你说更多？"

**注意**：评委这种话**最危险**——一表扬你容易松懈说错话。

**应对**：
> "谢谢您的认可。我们做的过程中也确实倾注了很多业务理解和工程严谨。
>
> 如果您方便，我特别想听**您觉得这个产品哪个细节最有价值**，以及**还有什么可以做得更好的**——这对我们后续优化方向很有帮助。"

把球踢回去，让评委说他喜欢什么、不喜欢什么——**这是有价值的反馈**，不是炫耀的机会。

---

## 演练流程（5/9 全员 30 min）

```
Step 1（5 min）：巧玲讲产品 5 分钟（按视频脚本走）
Step 2（15 min）：队员 A 当评委，从 50 题里随机抽 10 题狂问
   - 每题 30 秒答完 / 否则扣分
   - 答不上来站着思考 5 秒，主动说"这个我现场没准备好答案，请允许我会后回复"
Step 3（5 min）：队员 B 当"刁难评委"——专问 Q1/4/9/15/50 这种心理战
Step 4（5 min）：复盘 + 标记 5 题最差的，第二天再练
```

---

## 上台前 30 秒 cheat sheet（再贴一次）

```
开场 30 秒金句：
"我们解决一个真实痛点——业务方等数据等 1.7 天。
 用 Table-Talker，30 秒。这不是 demo，是产品级的 AI 原生 BI。"

紧张时记住 3 件事：
1. 慢一点说 → 思考时间
2. 数字精确 → 显得专业
3. 把球踢给评委 → 你不是 1 人答辩

紧急口诀：
"这个角度很有意思，我从 X 维度回答..."
"您说得对，我们 V1.1 的路线就是..."
"坦白说这是 trade-off，我们选 X 因为..."
```
