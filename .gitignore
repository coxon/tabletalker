# ============ 安全：Key 类文件绝不入库 ============
.env
.env.local
.env.*.local
**/.env
**/.env.local
*.pem
*.key
secrets.json

# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ============ Virtualenv ============
venv/
env/
.venv/
.env-venv/

# ============ Node ============
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ============ IDE / OS ============
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ============ 运行时产物 ============
logs/
*.log
eval_results/
reports/
# 离线生成，每次跑覆盖
schemas.json
# 大文件不入库（除非主办方公开数据集另行约定）
data/*.csv
data/*.parquet
data/*.xlsx

# ============ Docker ============
.dockerignore.local
volumes/

# ============ 设计稿临时文件 ============
canvas/preview/
*.bak

# ============ 早期遗留产物（已被 design-source 替代）============
frontend-demo/

# ============ Claude in Chrome 截图等临时输入 ============
img_v3_*.jpg
img_v3_*.png

# ============ 大文件 zip 不入库（设计稿初始 zip 等）============
*.zip
!sample_*.zip

# ============ 本地参考文档（不入仓库，含内网地址等敏感信息）============
推送到-GitAI.md
