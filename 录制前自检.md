# 录制前自检清单 · 5 分钟过一遍

> 演示前 10 分钟过一遍，每条都打 ✅。任何一条挂了就**别开始录**——重启再来。
> 录制目标：5 分钟成片，含配音 + 字幕。

---

## 一、环境层（5 项）

```zsh
bash stop.sh        # 先停干净
bash start.sh       # 一键起，等到 "🚀 Table-Talker 已启动"
bash warmup.sh      # 预热 7 个对话 + 评测
```

- [ ] `start.sh` 输出包含 ✅ venv 存在
- [ ] `start.sh` 输出包含 ✅ 后端启动成功
- [ ] `start.sh` 输出包含 ✅ 前端启动成功
- [ ] `start.sh` 输出包含 ✅ API 健康
- [ ] `warmup.sh` 输出包含 ✅ 7 个对话场景预热完成

---

## 二、浏览器层（8 项）

打开 http://localhost:8080/Table-Talker.html → ⌘+Shift+R 硬刷新。

- [ ] 顶部"Table-Talker · v1.0 · 亚信"出现
- [ ] 左侧 6 项导航（💬 对话 / 📊 看板 / 📄 报告 / 🗂 数据接入 / 🧪 批量评测 / ⚙ 设置）齐全
- [ ] 左侧 7 条历史对话齐全（c1 - c7）
- [ ] 默认进入对话页，**14 步 trace 流式自动播放**
- [ ] 答案 typewriter 逐字打出（不是瞬间显示）
- [ ] 圆环图 / 柱状图 / 折线图 渲染出来（不是空白）
- [ ] 异常洞察 3 条出现（红/黄/灰分级）
- [ ] 引用溯源可点开（"datasets / columns / sql / sample"齐全）
- [ ] DevTools Console **没有红色 Error**（只允许黄色 warning）

---

## 三、关键交互（10 项）

### 对话页（4 项）
- [ ] 点左侧栏切换对话（c2 HR 月报 / c3 ARPU）→ 14 步 trace 重新播放
- [ ] 顶部 toggle "商业 / 专家"切换 → 答案文本 + 追问建议同时变化
- [ ] 追问 chip 点击 → 可触发新对话（如点"对比西南区"）
- [ ] 点"📌 钉到看板" → toast "已钉到看板" → 1.2s 后跳到看板页

### 批量评测（3 项）
- [ ] 左侧切到"🧪 批量评测"
- [ ] 上传 `evaluation/sample_eval.jsonl` → **进度条 0/10 缓慢增长到 10/10**（5 秒）
- [ ] 6 个 metrics 卡片出现数字 + "⤓ results.jsonl" / "⤓ metrics.json" 可下载

### 看板/报告/数据/设置（3 项）
- [ ] 点"📊 看板" → 3 个分组（销售 / HR / 财务）卡片渲染正常
- [ ] 点"📄 报告" → 多页报告页面渲染（含目录、章节）
- [ ] 点"🗂 数据接入" → 6 个数据集卡片（含来源 datahub/mysql/api 等图标）

---

## 四、视觉细节（6 项）

- [ ] **关 DevTools**（F12 一下）—— 录制时不能露出
- [ ] **关 Tweaks 面板**（右下角浮窗如果展开了，收起来）
- [ ] 浏览器**全屏**（⌘+Ctrl+F 或 F11）
- [ ] 浏览器**收藏栏隐藏**（⌘+Shift+B）
- [ ] 浏览器 zoom 100%（⌘+0）
- [ ] 桌面**关闭所有通知**（系统设置 → 专注模式 → 勿扰）

---

## 五、音视频准备（5 项）

- [ ] **OBS / QuickTime** 已打开，分辨率 ≥ 1920×1080
- [ ] **麦克风**已测试（先录 3 秒回放确认音量）
- [ ] **配音稿**已打印 / 在另一台设备上备好（[`演示视频/演示视频脚本-5分钟.md`](./演示视频/演示视频脚本-5分钟.md)）
- [ ] **计时器**准备好（手机或 menubar 倒计时）
- [ ] **桌面壁纸切到纯色**（避免私人内容露出）

---

## 六、网络与日志（3 项）

- [ ] 后端日志 `tail -f .run/backend.log` 看着——录制时**不应有任何 error/warning** 滚出
- [ ] 浏览器 Network 标签看 `/api/chat/stream` 响应是 **200**（不是 422 / 500）
- [ ] 关闭代理 / VPN 软件（除非你需要 VPN 推 git）

---

## 七、灾备方案（万一翻车）

| 现象 | 立即操作 |
|---|---|
| 浏览器卡住没反应 | ⌘+R 刷新；不行 → ⌘+Shift+R |
| 后端崩溃 | `bash stop.sh && bash start.sh && bash warmup.sh` |
| Trace 没流出来 | DevTools Network 看 chat/stream 状态码；422 = chat.py 未 reload |
| 批量评测进度条不动 | 看 backend.log 有没有 SSE 错误；最差换 `?mock=true` URL |
| 网络抖动 | 切到 mock 模式：`MOCK_MODE=true` 一定要在 .env 里 |

---

## 八、录制时的 7 个动作要点

1. **节奏稍慢**：每个动作之间留 1-2 秒，便于后期剪辑
2. **鼠标移动平滑**：不要快速乱晃
3. **关键点停顿**：14 步 trace 跑完时停 1 秒让评委看清
4. **配音先录**：先把配音录好，再对着配音录屏（保证嘴对得上）
5. **字幕用剪映自动识别**，校对后内嵌
6. **不要反复重录小段**：成片有不连贯感；一次过完再剪
7. **录完先看一遍** 自己看哪里别扭，再决定要不要重录

---

## 九、提交前最终检查（录完视频后）

- [ ] 视频时长 ≤ 5:00
- [ ] 视频大小 ≤ 200MB（剪映导出选 1080p 中等码率）
- [ ] 含字幕（内嵌，不是外挂 .srt）
- [ ] 含配音（或字幕至少全程）
- [ ] 已放到 `演示视频/demo.mp4`
- [ ] 已 git add + git commit + git push

---

> 🎯 **打完所有 ✅ 再开始录** ——少打一项可能翻车 5 分钟。
