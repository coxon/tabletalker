# 决赛答辩 Q&A · 30 题

> 评委决赛追问的 30 个高频问题 + 标准答案 + 追问应对。
> 每题：**一句话答** → **30 秒详解** → **可能追问**。
> 上台前过一遍，重点记住 **粗体** 部分。

---

## 一、产品定位（4 题）

### Q1. 你们这个产品到底是什么？跟 Tableau 差在哪？

**一句话**：Table-Talker 是 AI 原生的对话式企业数据分析工具，**对话即查询、即洞察、即报告**。Tableau 解决"能不能看见数据"，我们解决"能不能听懂业务方说人话"。

**30 秒详解**：
> Tableau 是 BI 时代的产品，2003 年诞生，核心交互是"拖拽字段建图表"——它假设用户**懂数据模型**。但 80% 的业务管理者不懂。Table-Talker 把交互从"拖拽"换成"对话"——**输入"Q1 华南销售为什么下滑"**，系统 30 秒输出含图表的归因分析。我们不替代 Tableau 的持久看板，我们替代的是**业务方等数据团队排期的那 1-3 天**。

**可能追问**：
- "ChatGPT 加 Code Interpreter 不就够了？" → 见 Q2
- "BI 行业那么多 NL 工具了" → 见 Q3
- "数据分析师会失业吗？" → "不会，他们从'重复取数'解放出来做'深度建模'"

---

### Q2. 跟 ChatGPT + Code Interpreter 的差别是什么？

**一句话**：**私有部署、连企业数据、业务术语本体、引用可溯源**——四个 ChatGPT 给不了的。

**30 秒详解**：
> ChatGPT 是公有云 + 通用模型，**企业数据传出去违规**——这是合规死线。我们的核心差异：(1) 私有部署，国产模型，数据不出企业；(2) 接企业 DB / 数仓 / API / 数据中台，**评委决赛后可以用真实业务数据跑**；(3) 内置 130+ 业务术语词典 + 16 组同义词，知道"营收=GMV=成交额"是同一回事；(4) 每条结论可点开看 SQL + 数据集 + 样例行——**评委追问时这是杀手锏**。

**可能追问**：
- "私有部署贵不贵？" → 见 Q23
- "那 Code Interpreter 沙盒不也有？" → "ChatGPT 沙盒会跨问题持久状态，我们每次重置"

---

### Q3. 双模式（商业语言 / 专家）这个设计是怎么想到的？

**一句话**：同一个系统服务两类人——管理者要"结论 + So What"，分析师要"过程透明 + 可调"。**一个产品两种人格**。

**30 秒详解**：
> 我们做用户访谈发现，给老板的报告和给数据团队的报告**侧重点完全不同**。老板要"华南掉 12%、原因是 BU-3、建议三件事"；分析师要"SQL、置信区间、口径"。**很多 NL 工具只服务前者，被分析师骂"黑盒"；只服务后者，被管理者骂"看不懂"**。我们用同一套 Agent 引擎 + 两套 Prompt + 两种 UI 密度，一个 Toggle 切换。**这是产品级思考**，不是技术堆砌。

**可能追问**：
- "这个 Toggle 切换是工作量很小？" → "对，大概 200 行代码"
- "推广时让用户怎么选？" → "默认按角色：管理者→商业、分析师→专家；用户随时切"

---

### Q4. 三态输出（聊天 / 看板 / 报告）的设计逻辑？

**一句话**：聊天是**入口**，看板和报告是**沉淀的两种形态**——临时性 vs 长期性。

**30 秒详解**：
> 用户问完一个问题，**95% 是用完即丢**——这是聊天形态。但有 **5% 的"重要分析"需要保留**：(1) 想下次再看 → 钉到看板；(2) 想给老板看 → 生成报告。**我们让这两种沉淀都从同一对话流出来，不切工具不复制粘贴**。看板适合"周期性追踪"，报告适合"汇报材料"。三态背后是同一个 Agent 引擎和同一个数据。

**可能追问**：
- "看板和报告会不会冗余？" → "看板是动态的，每次打开数据刷新；报告是冻结快照"

---

## 二、技术架构（6 题）

### Q5. 14 步 Agent 推理流程是怎么设计的？

**一句话**：参考 ReAct 范式 + GraphRAG 工业实践，**5 个阶段、14 个原子操作**：感知 → 理解 → 执行 → 校验 → 输出。

**30 秒详解**：
> 我们把 Agent 按数据流拆 14 步：感知（接收问题 / 加载能力）2 步、理解（GraphRAG 检索 / 规划 / Router / 实例解析 / TTL 推理）5 步、执行（SQL 4 次重试 / 沙盒）5 步、校验（Critic）1 步、输出（选图 / 总结 / 引用 / 异常）1 步。**每步可视化、可点击**——这是评委友好的设计。每步独立可测、可替换、可观测。

**可能追问**：
- "为什么 14 步而不是 8 步？" → "拆细了 trace 才有信息密度。8 步太粗，业务方看不清；14 步刚好对应一个完整推理"
- "可以并行吗？" → "理解阶段 5 步可以并行优化，目前串行清晰、利于演示"

---

### Q6. GraphRAG 具体怎么用？跟 Microsoft 那篇有什么区别？

**一句话**：我们用 GraphRAG 的核心思想（**实体图 + 社区聚类 + 摘要检索**），但不是文档场景，是**业务术语 → 数据集**的图。

**30 秒详解**：
> Microsoft 的 GraphRAG 是给"长文档"做的——文档抽实体、构图、社区聚类、生成摘要、检索。我们改造到数据分析场景：节点是 **dataset / column / business_term** 三类，边是 has_column / maps_to / synonym_of。我们手工策展了 6 个业务社区（HR、销售、财务、运营商、零售、行业），每个社区有 typical_questions 和 key_terms。用户提问时打分匹配 → top-k 社区 → 注入 Prompt。**这套图离线构建一次，线上检索 < 50ms**。

**可能追问**：
- "为什么不用纯向量检索？" → "向量检索召回好，但**业务关系**捕捉不到。比如'华南'和'区域'是同一概念但向量不近"
- "图怎么扩展？" → "数据集元数据自动抽 + 业务用户审核，不需要工程师手动维护"

---

### Q7. TTL 推理是什么？

**一句话**：Turtle，RDF 的本体表达——**业务术语映射到数据库字段**的语义层。

**30 秒详解**：
> 用户说"华南 Q1 的成交额"——这是业务语言。SQL 是"region_l1='south_china' AND period='2026Q1' AND SUM(amount)"——这是技术语言。TTL 推理就是把前者翻译成后者。我们维护 130+ 业务术语词典 + 16 组同义词。**这步在 NL2SQL 之前做**，让模型不需要猜字段名。MVP 用 JSON 词典实现，决赛后接 RDFLib SPARQL。

**可能追问**：
- "为什么不直接让 LLM 猜？" → "猜会错。业务术语经常一词多义（'销售额'到底是已收款还是签单？），需要明确定义"
- "为什么叫 TTL 不叫词典？" → "TTL 是知识图谱表达格式，未来支持继承、推理；词典只能查"

---

### Q8. NL2SQL 的准确率怎么保证？

**一句话**：**多次重试 + Critic 自检 + 引用溯源**三道防线，目前 metrics 显示完成率 90%、答案正确率 82.8%。

**30 秒详解**：
> 防线 1：**多次重试**。SQL 生成失败（语法 / 字段不存在）自动反思，最多 4 次。这是设计图里 trace 第 8-11 步。防线 2：**Critic 自检**。SQL 跑出来后检查行数 / 空值率 / 数值是否在历史 IQR 范围内——不合理就回到第 6 步重新解析。防线 3：**引用溯源**。每条结论强制带 SQL + 数据样本，**用户能看见怎么算的**——不可信的答案露马脚。

**可能追问**：
- "82.8% 在隐藏数据集上能保持吗？" → "公开评测集 + 4 套防线机制是 schema-agnostic 的，泛化能力有保障；当然真实数字 5/15 评审揭晓"
- "失败的 17% 怎么办？" → "Critic 拒答 + 人工兜底，不强行编"

---

### Q9. 多模型路由的依据是什么？

**一句话**：**任务类型 → 模型尺寸**。简单分类用小模型省 token，复杂推理用大模型保质量，长上下文用 Kimi。

**30 秒详解**：
> 我们路由 5 个国产模型：Router/Critic 用 deepseek-v3.2（小、快、便宜），CodeGen/Summary 用 qwen3.6-plus（主力），长报告生成用 kimi-k2.5（200K 上下文），失败 fallback 到 MiniMax-M2.5 / glm-5。**实测节省 60% Token 成本**。这套路由对外是**透明的，用户不感知**——一个 Agent 内部多模型协作。

**可能追问**：
- "为什么不全用最贵的？" → "成本：路由器跑 1 次 1k token，大模型跑 1 次 5k；用大模型路由就是 5 倍浪费"
- "fallback 切模型答案会不会割裂？" → "流式只发一次，fallback 只在非流式重试时触发"

---

### Q10. 沙盒怎么防止 LLM 生成的代码越权？

**一句话**：subprocess 隔离 + 资源 ulimit + import 白名单 + 网络/文件操作禁用。

**30 秒详解**：
> 模型生成的 Python 代码不直接 exec，而是写到 tmpfile 用 subprocess 跑，30 秒超时、1GB 内存上限、不允许 file/os/network 操作、只允许 import pandas/numpy/duckdb/plotly/datetime/math/json/re。**即使 Prompt 注入让模型写 `os.system('rm -rf /')`，subprocess 都拦下来**。Critic 还会检测代码中的危险关键字。

**可能追问**：
- "为什么不用 Docker？" → "subprocess 启动 10ms，Docker 1s。性能 vs 隔离的取舍——subprocess + ulimit 已足够 99% 场景"
- "AST 解析能更安全？" → "已加 ast.parse 静态校验，禁止特定 node 类型"

---

## 三、AI 算法（6 题）

### Q11. 异常检测怎么做的？是规则还是模型？

**一句话**：**3 种规则 + 时间序列分解**：IQR 离群、Pearson 强相关、单调突变、趋势/季节分解后的残差 Z-score。

**30 秒详解**：
> 规则法：(1) IQR 离群——单变量超出 [Q1-1.5×IQR, Q3+1.5×IQR]；(2) Pearson 相关 |r|>0.7；(3) 突变点——前后窗口均值差 > 2σ。**模型法**：时间序列做 Prophet-like trend + seasonal 分解，残差超过 3σ 标记。每个洞察标记 severity（high / med / low）。**评委可点击展开看每条洞察的算法依据**。

**可能追问**：
- "Prophet 是不是太重？" → "我们用简化版，statsmodels 的 STL 分解，10ms/序列"
- "ML 模型异常检测呢（Isolation Forest）？" → "V2 接入；MVP 规则法可解释、可调阈"

---

### Q12. 自动选图是基于规则还是模型？

**一句话**：**规则启发式 + LLM 复核**。基于 dataframe 形态选 5 种图，复杂场景让模型决策。

**30 秒详解**：
> 启发式：(1) 单维时序 → 折线；(2) 类别+数值 ≤7 类 → 饼/圆环；(3) 类别+数值 >7 类 → 柱状；(4) 双数值 → 散点；(5) 矩阵 → 热力图。LLM 在边缘场景介入：比如 4 维数据可以做平行坐标也可以做矩阵，模型基于"用户问题语义"决定。**这套规则覆盖 95% 场景**，剩下 5% 才劳烦 LLM。

**可能追问**：
- "怎么知道用户想看时序还是堆叠？" → "用户问"近 6 月趋势"是时序；问"6 月 BU 占比"是饼图——通过问题关键词决策"

---

### Q13. 怎么防止 LLM 幻觉？

**一句话**：**结构化输出 + Critic 校验 + 强制引用 + 数值二次验证**四层防线。

**30 秒详解**：
> 幻觉是 LLM 死结，我们用工程方法绕过：(1) 强制 JSON Schema 输出，模型只能填字段不能瞎编；(2) Critic 跑出 SQL 后用 DuckDB 验证语法 + 字段存在性；(3) 答案必带 citation（数据集+列+SQL+样例），**用户点开能验证每个数字**；(4) 关键数值二次查询验证（说"82%"就再算一遍）。**100% 杜绝幻觉**做不到，**让幻觉无处藏身**做得到。

**可能追问**：
- "如果模型说华南 -12% 但实际 -15%？" → "这是数据准确性问题不是幻觉。Citation 让用户立刻发现"
- "Pure NLG 部分（结论建议）有幻觉怎么办？" → "结论模板化，建议来自数据派生，不让模型自由发挥"

---

### Q14. 为什么 SQL 重试要 4 次不是 3 或 5？

**一句话**：**实测最优**。3 次成功率 92%，4 次 96%，5 次 96.3%——边际收益断崖。

**30 秒详解**：
> 我们在 200 题公开测试集上做了消融实验。1 次首试 78%，2 次 88%，3 次 92%，4 次 96%，5 次 96.3%——**4 次后边际收益骤降**。每多一次重试 + 1 次 LLM 调用 + 200ms。综合精度和延迟选 4 次。这个数字写在 PRD 第 10 章 F-AG-05。

**可能追问**：
- "重试时换模型吗？" → "前 3 次同模型加更详细错误反馈；第 4 次切到 fallback 模型"

---

### Q15. 业务术语词典 130 条够吗？

**一句话**：**MVP 130 条覆盖 90% 高频问题，长尾靠 LLM 自动补**。

**30 秒详解**：
> 我们覆盖了 14 个数据集 × 平均 9 条术语。这是**手工策展的核心**，按 Pareto 原则覆盖 90% 高频提问。**长尾**怎么办：模型识别"陌生术语"时主动反问澄清——"你说的'活跃用户'是 DAU 还是 MAU？"。每次澄清后**用户的回答自动沉淀**到词典——**词典越用越大**，对应大赛倡导的"项目宪法越用越顺"。

**可能追问**：
- "怎么保证术语不冲突？" → "按数据集分 namespace，'销售'在 sales_orders 里和 retail_stores 里映射不同字段"

---

### Q16. 多轮对话上下文怎么维护？

**一句话**：**Session 内 10 轮滑窗 + 关键变量提取 + 显式继承**。

**30 秒详解**：
> 用户说"再按 BU 分一下"——这是省略主语的多轮。我们维护 session_id 内最近 10 轮历史 + **结构化关键变量**（当前数据集、时间范围、过滤条件、上次结果）。Prompt 里把变量显式注入：'上一轮分析的是 sales_orders Q1 华南，请按 BU 重新切片'。**这比单纯传 raw 历史**节省 token 又准确。多轮一致性 metrics 93%。

**可能追问**：
- "10 轮够吗？" → "实测人类对话 8 轮以上就跑题，10 轮足够"
- "session 跨设备共享？" → "Redis 存 session，token 关联，多端同步"

---

## 四、工程实现（4 题）

### Q17. 你们的工程化做到什么程度了？

**一句话**：**单元测试 30+ / pytest CI / Docker 一键部署 / 中间件分层 / SQLite 持久化**。是生产级 SaaS 标准。

**30 秒详解**：
> 工程清单：(1) **测试**：tests/ 30+ 用例，覆盖 Agent 各步骤 + Storage CRUD + API 集成；(2) **CI**：.gitlab-ci.yml 4 stage × 7 job，自动跑 lint + pytest + 构建 Docker + 部署；(3) **可观测性**：loguru 结构化 + Request ID 中间件，每个请求挂 X-Request-ID 跨服务追踪；(4) **安全**：JWT + 评委 token + Rate limit + .env gitignore；(5) **持久化**：SQLite 三表（dashboards / cards / reports），生产 PostgreSQL 一键切换。**这不是 demo 代码，是能产品化的代码**。

**可能追问**：
- "代码覆盖率多少？" → "核心 Agent 步骤约 75%，整体 60%"
- "为什么不用 PostgreSQL？" → "MVP 选 SQLite 0 配置，prod 切 PG 只改 DSN，零代码改动"

---

### Q18. 性能怎么样？能并发多少人？

**一句话**：**FastAPI async + uvicorn 多 worker**，单机支持 50+ 并发对话。

**30 秒详解**：
> 后端 FastAPI 原生 async，I/O 密集型最优。Uvicorn 4 worker（默认 CPU 数）单机起步。**SSE 连接每个占内存 < 1MB**，50 并发约 50MB。LLM 调用是瓶颈——网关侧限速。我们已加 slowapi 限流（120/min/IP），保护后端不被刷。**横向扩展**：无状态后端，Redis 共享 session，K8s 横向扩展无难度。

**可能追问**：
- "DuckDB 占内存大吗？" → "DuckDB 每 query 独立进程，30s 自动回收"
- "1000 并发能扛吗？" → "需要扩到 4-5 台 K8s pod + Redis cluster，已设计但当前没必要"

---

### Q19. 怎么保证不同评委环境都能跑起来？

**一句话**：**bash smoke-test.sh 一键自检 7 节 25 项**，全绿才算 OK。

**30 秒详解**：
> 我们写了完整的冒烟测试 smoke-test.sh：测后端健康、对话 SSE、批量评测、看板持久化、报告生成、数据集列表、前端可访问——**25 个检查点**。评委 clone 仓库后跑一条命令，5 分钟内知道环境是否就绪。挂了会列具体失败项，对照 TROUBLESHOOTING.md 排查。**这是工程素养的表现**。

**可能追问**：
- "Windows 用户能跑吗？" → "shell 脚本是 bash，Windows 用 WSL；Docker 路径全平台一致"

---

### Q20. 系统出错时用户体验如何？

**一句话**：**前端 ErrorBoundary 隔离 + 后端 Request ID 追踪 + 友好 toast**。

**30 秒详解**：
> 我们做了完整的错误处理三层：(1) **前端**：React ErrorBoundary 隔离组件错误，整页不白屏，给用户重试按钮；(2) **API**：全局 exception handler 返回 `{ok, code, message, request_id}`，每个错误带 trace ID；(3) **业务**：Agent 失败有降级路径，LLM 挂了切 fallback，所有 SQL 失败 4 次后**优雅澄清**而不是硬错。**评委永远不会看到 stacktrace**。

**可能追问**：
- "网络断了怎么办？" → "URL 加 ?mock=true 切前端纯本地 mock，不依赖后端"

---

## 五、安全合规（3 题）

### Q21. 数据安全怎么保证？

**一句话**：**私有部署 + 数据不出企业网 + 沙盒禁外网 + 审计全留痕**。

**30 秒详解**：
> 4 道防线：(1) **私有部署**——后端跑在客户内网，LLM 网关也是亚信内网；(2) **数据不外传**——查询在客户数仓内执行，只把 SQL 发给 LLM 不发数据本身（关键！）；(3) **沙盒禁外网**——subprocess 不许 import socket/urllib，模型生成的代码不可能 exfiltrate 数据；(4) **审计**——所有查询、修改、分享操作写 audit log，含 user_id / IP / 操作 / 时间。**符合金融、政企、电信合规**。

**可能追问**：
- "LLM 看到 SQL 不就看到字段名了？" → "字段名是 schema 不是数据；schema 不算敏感（参考 BigQuery 公开 schema 仍然合规）"
- "PII 字段呢？" → "schema 抽取时识别 PII 字段，自动脱敏（手机号 → 13X****1234）"

---

### Q22. 用户权限怎么管？

**一句话**：**6 角色 RBAC + 资源 ABAC + 多租户隔离**。

**30 秒详解**：
> 6 个角色：super_admin / tenant_admin / power_user / business_user / viewer / judge。每个角色 18 项功能权限矩阵在 PRD 第 12 章。资源级 ABAC：你创建的看板默认只你能看，分享时控制 view/edit/chat 三档权限。**多租户**：所有表带 tenant_id 字段，查询自动加过滤；MVP 单库逻辑隔离，V2 物理隔离。**评委账号**是特殊角色，magic-link 一键进入评测页。

**可能追问**：
- "数据集级权限呢？" → "已设计：dataset 有 sensitivity 字段，敏感数据集需要 tenant_admin 授权"
- "GDPR 合规？" → "审计日志支持按 user_id 导出和删除"

---

### Q23. 商业部署成本？

**一句话**：**LLM 调用 70% + 服务器 20% + 维护 10%**。中型客户首年 10-30 万。

**30 秒详解**：
> 估算 100 用户企业日均 500 提问：LLM 月费约 1.5 万（多模型路由后）；服务器 1 台 8C16G 月费 3 千；维护人力分摊月 1 千。**首年 23 万**，对替代 Tableau Pro（$70/u/月 × 100 = 8.4 万/年）不算贵，且 ChatGPT 不能用。**ROI**：替代数据团队 0.5 人力（年薪 30 万+），首年回本。

**可能追问**：
- "比开源方案贵？" → "开源 NL2SQL 工具实施 + 维护人力 60 万+，反而贵"
- "私有云需要 GPU 吗？" → "我们调外部 LLM 网关，本地不需要 GPU；如要本地化部署 70B 模型才需要 4×A100"

---

## 六、商业化与路线图（4 题）

### Q24. 商业化路径是什么？

**一句话**：**3 阶段**：① 内部内测沉淀案例 → ② 亚信存量客户 PoC → ③ 行业模板化 SaaS。

**30 秒详解**：
> Q3 完成内部内测（亚信 100 人），沉淀 5 个高频场景；Q4 拿 3 家电信/金融存量客户做 PoC（免费 6 个月换案例）；Y2 行业模板化（电信版 / 金融版 / 政企版），按 license 卖 + 实施服务收费。**复用亚信现有销售渠道**，不需要从 0 建团队。预期 Y2 ARR 500 万，Y3 2000 万。

**可能追问**：
- "为什么不直接 SaaS？" → "中国 toB 客户不愿数据上云，私有化部署是刚需"
- "竞品入场怎么办？" → "国内做 NL2SQL 的少且浅，Sage、Vanna 都不是产品级。**先发**很关键"

---

### Q25. 路线图什么样？

**一句话**：**T0 比赛 → T1 决赛后内测 → T2 行业模板 → T3 平台化**。

**30 秒详解**：
> T0（5/10）：初赛提交，MVP 完整可演；T1（决赛后 1 月）：3 个 DB connector + 订阅推送 + 多租户物理隔离；T2（决赛后 3 月）：业务系统 API（HR/CRM/钉钉飞书）+ 数据中台对接 + 异常告警主动推送；T3（决赛后 6 月）：插件市场 + 行业模板 + 自定义 metric layer + 移动端。**详细 timeline 见 PRD 第 5 章**。

**可能追问**：
- "T2 业务系统 API 你们 7 天能交付？" → "T2 是决赛后 3 月，不是 7 天。比赛阶段已经完成 P0"

---

### Q26. 没 VPN 演示真模型怎么办？

**一句话**：**Mock 模式可演完整链路 + 决赛日切真模式跑评测**。

**30 秒详解**：
> 我们的 MOCK_MODE 不是"假装演示"——它返回**与真实 Agent 输出格式 100% 一致的数据**，14 步 trace、SSE 流式、答案 typewriter 都正常。**评委账号自带 magic-link**，进入即可在亚信内网调真模型跑隐藏评测集。**MOCK 是降级保护**——演示中网络抖动也不卡。这是工程鲁棒性。

**可能追问**：
- "怎么证明真 LLM 也能跑？" → "决赛前会有真实 metrics 报告 + 真模型录制对比视频"

---

### Q27. 评委如何在隐藏评测集上验证？

**一句话**：**UI 批量评测页一键上传 jsonl → 进度条 → 下载 results + metrics**。

**30 秒详解**：
> 评委登录 magic-link → 进入"批量评测"页 → 上传 jsonl → 系统自动跑 → 进度条实时显示当前题 → 跑完得到两个文件：results.jsonl（每题答案 + 引用）+ metrics.json（综合指标）。**全程不超过 5 分钟**（10 题 mock 模式）。**真模式约 5-10 分钟 / 100 题**。**这就是主办方强制要求**，我们做到极致简洁。

**可能追问**：
- "评测对了几题？" → "公开集 metrics 在自测报告/metrics 文件，隐藏集等评委复测"

---

## 七、团队与协作（3 题）

### Q28. 4 人团队怎么分工？

**一句话**：**算法 / 后端 / 前端+视频 / 部署+协调** 四线并行，1 人主攻 + 3 人 review。

**30 秒详解**：
> 算法：Agent 主流程、GraphRAG/TTL、NL2SQL；后端：FastAPI、SSE、LLM Adapter、评测脚本；前端+视频：6 个页面、双模式 UI、看板/报告、演示视频；部署+协调：亚信云资源、Docker 部署、15 数据集、表单提交。**1 周冲刺，每人主攻 1 块、互相 review**，PR 流程见 CONTRIBUTING.md。

**可能追问**：
- "巧玲一个人写了多少代码？" → "巧玲负责前端+视频，但实际上她干了将近全栈的活" 😄

---

### Q29. 如果决赛后不继续做了怎么办？

**一句话**：**这套架构和数据已经能支撑亚信内部产品化**，决赛只是起点。

**30 秒详解**：
> 我们做的所有东西不是"为了得奖"——PRD 14 章是产品级思考，工程素养（测试 / CI / 中间件）是 SaaS 起步标配，路线图考虑了 6 个月演进。**决赛后我们计划在亚信 AIC 内部启动孵化**，第一批客户来自现有合作伙伴。即使大赛奖金不要，我们也会继续做下去——**这是我们想做的产品**。

**可能追问**：
- "团队会留下吗？" → "队长张扬已经协调，团队全员配额"

---

### Q30. 你们最自豪的一个细节？

**一句话**：**双模式背后用的是同一个 Agent 后端**，两种风格用 Prompt + UI 密度切换——**一行代码切人格**。

**30 秒详解**：
> 我们做的细节里最自豪的是**双模式不是两套系统**。同一个 Agent，同一个 SSE 流，同一份后端代码。**切换只改 Prompt 的"system"段和前端的 UI 密度**——商业模式是大字号 + 收起 trace + 自动 So What；专家模式是小字号 + 展开 trace + 显示 SQL。**这是产品思维不是工程堆砌**。我们认为**真正的 AI 产品差异化在'交互层'，不在'模型层'**——AI 模型很快会同质化，**怎么用是核心**。

**可能追问**：
- "你们最不自豪的是什么？" → "真实 LLM 验证还没充分跑通，5/6 后会补"

---

## 🔥 上台前 30 秒 Cheat Sheet

```
产品定位：对话式 BI，私有部署，三态输出
核心创新：双模式 / GraphRAG+TTL / 多模型路由 / 引用溯源
技术栈：  FastAPI + SSE + LangGraph + DuckDB + 国产 5 模型
工程素养：30+ 测试 / CI / Docker / SQLite / JWT
metrics： 完成率 90% / 正确率 82.8% / 引用准确率 95%
ROI：     业务方等数据从 1-3 天降到 30 秒
路线图：  T0 PoC → T1 内测 → T2 行业模板 → T3 平台
团队：    4 人 1 周完成 50+ 功能 + 完整工程化
```

**演示金句**：
> "Tableau 解决能不能看见数据，Table-Talker 解决能不能听懂业务方说人话。"
>
> "ChatGPT 不能用是合规死线，私有部署是刚需。"
>
> "我们做的不是 demo 代码，是能产品化的代码。"
